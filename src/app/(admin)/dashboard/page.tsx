/**
 * Dashboard administrativo (Sprint 1: fundação).
 *
 * Os indicadores reais (notícias publicadas, em revisão, agendadas,
 * empresas cadastradas, campanhas ativas) serão implementados junto
 * ao módulo `editorial` e `empresas`, conforme 01.03.1 (seção 3).
 */
export default function DashboardPage() {
  return (
    <div>
      <h1 className="mb-4 text-2xl font-semibold">Dashboard</h1>
      <p className="text-sm text-gray-500">
        Fundação do painel administrativo concluída. Os indicadores editoriais
        serão exibidos aqui a partir do Sprint 2.
      </p>
    </div>
  );
}
